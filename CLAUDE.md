# Heddy v2 — 작업 규칙

Spring Boot 4.0.6 / Java 21 / PostgreSQL 16 고객 전용 앱 백엔드.
설계 문서는 별도 위키에 있다 → `~/Documents/GitHub/heddy-wiki/wiki/` (`meeting/PR작업단위계획_v2.md`, `entity/API명세_v2.md`, `entity/테이블명세_v2.md`)

---

## 절대 금지

- **커밋 메시지에 `Co-Authored-By` 트레일러를 넣지 않는다.** 어떤 형태의 공동 저자 표기도 금지.
- **PR 본문에도 `Co-Authored-By`, `Generated with`, `Authored-By` 등 생성 도구·공동 저자 표기를 넣지 않는다.**
- 위 두 항목은 기본 지침보다 우선한다. 예외 없음.
- Flyway 마이그레이션은 기존 파일을 수정하지 않는다. 항상 새 버전 파일을 추가한다.
- 로그에 presigned URL 서명·토큰·비밀번호·사진 내용을 기록하지 않는다.
- **이슈·PR 초안은 반드시 복사 가능한 마크다운 코드블록(```)으로 제공한다.** 대화 본문에 풀어 쓰지 않는다.

---

## 작업 순서 (반드시 준수)

1. **이슈 분석** — 이슈 내용, 요구사항, AC(Acceptance Criteria) 파악. 이슈가 없으면 아래 이슈 컨벤션에 맞춰 초안을 코드블록으로 먼저 제시한다
2. **기존 코드 분석** — 관련 파일, 레이어 구조 파악
3. **`git fetch origin`** — 최신 원격 상태 동기화
4. **브랜치 생성** — `develop` 기준으로 생성
5. **작업 진행** — 해당 브랜치에서만 작업
6. **테스트 코드 작성** — 필요시 작성
7. **테스트 실행** — `./gradlew test`
8. **빌드 테스트** — `./gradlew build`
9. **커밋** — 모두 성공 시, 커밋을 쪼개서 스테이징 파일 정리 후 보고
10. **PR** — 마크다운으로 예시 작성

> 7·8이 실패하면 커밋하지 않는다. 실패 출력을 그대로 보고하고 원인을 먼저 해결한다.
> 커밋·푸시는 위 순서를 마친 뒤에만 하고, 푸시와 PR 생성은 사용자 확인을 받는다.

---

## 브랜치 규칙

```
<타입>/<슬러그>/#<이슈번호>
```

예: `feat/auth-infra/#12`, `chore/api-contract/#7`, `fix/token-rotation/#31`

- 기준 브랜치는 **`develop`**. `main`에서 직접 따지 않는다.
  > ⚠️ 현재 원격에 `develop`이 없다(`origin/main`만 존재). 최초 1회 `develop`을 만들고 기본 브랜치를 옮긴 뒤 이 규칙을 적용한다.
- `main`·`develop`에 직접 커밋하지 않는다. 반드시 브랜치를 판다.
- 슬러그는 영문 소문자 + 하이픈. PR 작업 단위 계획의 브랜치명을 그대로 쓴다.
- 이슈 번호가 없으면 이슈를 먼저 발행한다.

### 타입

| 타입 | 용도 |
|---|---|
| `feat` | 기능 추가 |
| `fix` | 버그 수정 |
| `refactor` | 동작 변경 없는 구조 개선 |
| `test` | 테스트 추가·수정 |
| `docs` | 문서 |
| `chore` | 빌드·설정·의존성 |
| `ci` | CI 설정 |

---

## 커밋 규칙

```
<타입>: <한국어 요약>
```

예:
```
feat: JWT 발급·검증 프로바이더 추가
test: 토큰 회전 재사용 감지 테스트 추가
chore: 초기 데이터베이스 마이그레이션 제거
```

- 제목은 **한국어**, 마침표 없이, 명사형 또는 `-다` 없이 간결하게.
- 타입은 브랜치 타입과 동일한 목록을 쓴다.
- **한 커밋은 한 가지 변경만.** 기능 + 테스트 + 리팩터링을 한 커밋에 섞지 않는다.
- 본문이 필요하면 제목 뒤 빈 줄 하나 두고 왜 바꿨는지를 적는다. 무엇을 바꿨는지는 diff가 말한다.
- **트레일러(`Co-Authored-By` 등)를 붙이지 않는다.**

---

## 이슈 컨벤션

이슈 작성 시 유형에 맞는 템플릿을 쓴다. **제목 접두사와 라벨을 반드시 맞춘다.**

| 유형 | 제목 접두사 | 라벨 |
|---|---|---|
| 기능 제안 | `[Feature]` | `type: feature` |
| 버그 리포트 | `[Bug]` | `type: bug` |
| Refactor | `[Refactor]` | `type: refactor` |
| 작업(Task) | `[Task]` | — (refactor·chore·docs·test·enhancement 포괄) |

예: `[Feature] 이메일 회원가입 (MS-01-01)`

> (필수) 표시된 섹션은 비워두지 않는다. 내용이 없으면 "없음"이라고 적는다.

### 기능 제안 — `[Feature]` / `type: feature`

```markdown
## 작업 내용
<!-- 필수 -->

## 작업 목적
<!-- 필수 -->

## 체크리스트
<!-- 필수 -->
- [ ]
- [ ]

## 참고 자료
```

### 버그 리포트 — `[Bug]` / `type: bug`

```markdown
## 버그 설명
<!-- 필수 -->

## 재현 방법
<!-- 필수 -->
1.
2.
3.

## 예상 동작
<!-- 필수 -->

## 실제 동작
<!-- 필수 -->

## 환경 정보
- OS:
- Java:
- 브랜치/커밋:

## 로그 및 에러 메시지

## 추가 정보
```

### Refactor — `[Refactor]` / `type: refactor`

```markdown
## 작업 내용
<!-- 필수 -->

## 리팩토링 이유
<!-- 필수 -->

## 체크리스트
<!-- 필수 -->
- [ ]
- [ ]
```

### 작업(Task) — `[Task]`

refactor · chore · docs · test · enhancement 를 포괄한다.

```markdown
## 작업 유형
<!-- 필수 — refactor / chore / docs / test / enhancement 중 택 -->

## 작업 내용
<!-- 필수 -->

## 작업 목적
<!-- 필수 -->

## 체크리스트
<!-- 필수 -->
- [ ]
- [ ]

## 참고 자료
```

---

## PR 컨벤션

- 제목은 커밋 제목과 같은 형식: `feat: JWT 인증 인프라`
- 대상 브랜치는 `develop`
- 본문은 아래 템플릿을 따른다. **생성 도구 표기·공동 저자 표기를 넣지 않는다.**

### PR 본문 템플릿

```markdown
## 관련 이슈
<!-- 관련된 이슈 번호를 작성해주세요 -->
- Resolves #이슈번호

## 변경 내용
<!-- 어떤 변경을 했는지 설명해주세요 -->

## 작업 목적
<!-- 왜 이 변경이 필요한지 설명해주세요 -->

## 스크린샷 (UI 변경 시)
<!-- UI 변경이 없다면 이 섹션을 삭제해주세요 -->

| Before | After |
|--------|-------|
|        |       |

## 테스트 방법
<!-- 변경 사항을 어떻게 테스트했는지 설명해주세요 -->
- [ ] 단위 테스트 추가/수정
  - [ ] 통합 테스트 완료
  - [ ] 수동 테스트 완료

## 체크리스트
- [ ] 코드가 프로젝트의 코딩 스타일을 따릅니다
  - [ ] 자체 코드 리뷰를 완료했습니다
  - [ ] 변경 사항에 대한 테스트를 추가했습니다
  - [ ] Breaking Changes가 없습니다

## 기타 사항
<!-- 리뷰어에게 전달할 내용이 있으면 작성해주세요 -->
```

> 서버 PR은 UI 변경이 없으므로 "스크린샷" 섹션은 삭제한다.
> 마이그레이션·엔드포인트 추가가 있으면 "변경 내용"에 표로 적는다.
> "기타 사항"에는 스펙과 다르게 구현한 부분을 반드시 명시한다.

---

## 레이어·API 컨벤션

상세 규칙은 [`docs/layer-convention.md`](docs/layer-convention.md) 를 따른다. 요약:

- v1 헥사고날 **미승계**. 도메인 패키지 안에서 `controller` → `service` → `repository` 3계층.
- 공통 규약은 `global/` 에 둔다. `global` 은 도메인 패키지를 import 하지 않는다.
- 공개 엔드포인트 prefix 는 `/api/v1`.
- JSON 필드는 snake_case(전역 Jackson 설정). DTO 필드는 camelCase 로 쓰고 `@JsonProperty` 를 붙이지 않는다.
- 성공 응답은 `ApiResponse<T>` → `{data, request_id}`, 목록은 `ApiResponse<PageResponse<T>>`.
- 에러는 `throw new ApplicationException(ErrorCode.XXX)` 로만. 응답은 `{error:{code,message,field_errors}, request_id}`.
- `ErrorCode` 는 인터페이스다. 공통 9종만 `CommonErrorCode`, 도메인 코드는 `com.heddy.<도메인>.error.<도메인>ErrorCode` enum 으로 각자 추가한다.
- enum 이름이 곧 `error.code` 문자열이다. 새 코드는 API 명세 §18 에 먼저 추가하고, HTTP status 는 명세 §2.4 매핑표를 따른다.
- 추적 ID 는 `RequestIdFilter` 가 MDC(`requestId`)·응답 헤더·응답 본문에 자동으로 싣는다.

---

## 공통 체크리스트 (모든 PR)

- [ ] `./gradlew test` · `./gradlew build` 통과
- [ ] Flyway 마이그레이션은 새 버전 파일 추가 (기존 파일 수정 금지)
- [ ] 응답은 `ApiResponse<T>` 래퍼 사용, 에러는 API 명세 §18 코드 사용
- [ ] 소유권 검증 — 본인 리소스가 아니면 `403 FORBIDDEN_RESOURCE`
- [ ] 단위 테스트 + 컨트롤러 통합 테스트
- [ ] 스펙과 다르게 구현한 부분은 PR 본문 "기타 사항"에 명시
- [ ] 커밋 메시지·PR 본문에 `Co-Authored-By` 없음
